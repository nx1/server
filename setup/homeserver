server {
    listen 80;
    server_name pi pi.tailscale.net; # Use 'pi' and the optional Tailscale domain

    location / {
        include proxy_params;
        proxy_pass http://127.0.0.1:5000;
    }
}
